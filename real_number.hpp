#pragma once

#include <cstdint>

namespace water {
	namespace real_number {
		enum class operator_enum : int32_t {
			add,
			sub,
			div,
			mul,
			sqrt,
			neg,
		};

		class real_number {
		public:
		private:

		};
	}
}